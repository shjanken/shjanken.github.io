---
    layout: post
    title: VIM技巧学习笔记
    category: vim
---


1. 使用 `Vr` 可以替换一整行所有文本。
2. 使用 `gv` 可以重新选择上次选择的高亮选区
3. `netrw` 是内置的目录浏览器
4. 选定制订文本：
    1. 使用 `a`+`分隔符` : 可以选择包括分隔符在内的所有文本。
    2. 使用 `i`+`分隔符` : 可以选择分隔符内的文本（不包括分隔符本身） 
